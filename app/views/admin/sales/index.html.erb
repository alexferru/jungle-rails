<section class="admin-sales-index">

  <header class="page-header">
    <h1>Admin &raquo; All Sales</h1>
  </header>

  <div class="d-flex justify-content-end mb-4">
    <%= link_to '+ New Sale', [:new, :admin, :sale], class: 'btn btn-success' %>
  </div>
  

  <div class="panel panel-default sales">

    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Name</th>
          <th>Start Date</th>
          <th>End Date</th>
          <th>Status</th>
          <th>Percent Off</th>
          <th></th>
        </tr>
      </thead>
      <tbody>

        <% @sales.each do |s| %>
          <%= render 'sale', sale: s %>
        <% end %>
      </tbody>
    </table>

  </div>

</section>